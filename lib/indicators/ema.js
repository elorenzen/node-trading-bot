

    // Beginning of EMA testing
    /*
    const now = new Date();
    const year = now.getFullYear()
    const date = now.getDate()
    const month = moment().format('L').slice(0, 2)
    console.log('tickers: ', tickers)
    console.log('first ticker: ', tickers[5])
    const time = moment().format('LT').slice(0,5)
    const formattedTime = `${year}-${month}-${date} ${time}`
    console.log('formatted time: ', formattedTime)
    ema(tickers[0])
    */
    // End of EMA testing